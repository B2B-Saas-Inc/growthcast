import { describe,expect,it } from 'vitest';
import { forecast,type Assumptions } from './forecast';
const a:Assumptions={months:2,monthlyTrafficGrowth:.1,signupRate:.1,purchaseRate:.2,voluntaryCustomerChurn:.03,delinquentCustomerChurn:.01,voluntaryRevenueChurn:.03,delinquentRevenueChurn:.01,expansionRate:.02,retractionRate:.01,newCustomerArpu:40,grossMargin:.8,targetLtvCac:3,monthlyIncrementalVisitors:0,monthlySalesMarketingOverhead:0};
describe('forecast',()=>{it('calculates a traceable monthly bridge',()=>{const [m]=forecast({month:'2026-07',visitors:1000,customers:100,mrr:4000},a);expect(m.month).toBe('2026-08');expect(m.visitors).toBe(1100);expect(m.signups).toBe(110);expect(m.newCustomers).toBe(22);expect(m.churnedCustomers).toBe(4);expect(m.customers).toBe(118);expect(m.endingMrr).toBe(4760);expect(m.arr).toBe(57120);expect(m.maxCostPerSignup).toBeGreaterThan(0);expect([m.visitors,m.signups,m.newCustomers,m.churnedCustomers,m.customers].every(Number.isInteger)).toBe(true)});it('adds channel traffic once at go-live, then compounds it',()=>{const months=forecast({month:'2026-07',visitors:1000,customers:100,mrr:4000},{...a,months:3},[{name:'Meta',visitors:10000,goLiveMonth:2,signupRate:.1,purchaseRate:.2,arpu:40}]);expect(months[0].visitors).toBe(1100);expect(months[1].visitors).toBe(11210);expect(months[2].visitors).toBe(12331)});it('applies monthly visitor overrides as adjustments to the compounded channel cohort',()=>{const months=forecast({month:'2026-07',visitors:0,customers:0,mrr:0},{...a,months:2,monthlyTrafficGrowth:.1},[{name:'Meta',visitors:100,goLiveMonth:1,signupRate:.1,purchaseRate:.2,arpu:40}],{channelVisitors:{'2026-08':{Meta:100},'2026-09':{Meta:40}}});expect(months[0].visitors).toBe(100);expect(months[1].visitors).toBe(150)});it('excludes channels with live month zero',()=>{const base=forecast({month:'2026-07',visitors:1000,customers:100,mrr:4000},{...a,months:2});const disabled=forecast({month:'2026-07',visitors:1000,customers:100,mrr:4000},{...a,months:2},[{name:'Meta',visitors:10000,goLiveMonth:0,signupRate:.1,purchaseRate:.2,arpu:40}]);expect(disabled).toEqual(base)});it('applies monthly channel and churn overrides to later model results',()=>{const base=forecast({month:'2026-07',visitors:1000,customers:100,mrr:4000},a,[{name:'Meta',visitors:100,goLiveMonth:1,signupRate:.1,purchaseRate:.2,arpu:40}]);const changed=forecast({month:'2026-07',visitors:1000,customers:100,mrr:4000},a,[{name:'Meta',visitors:100,goLiveMonth:1,signupRate:.1,purchaseRate:.2,arpu:40}],{channelVisitors:{'2026-08':{Meta:1000}},revenueChurn:{'2026-08':.1}});expect(changed[0].visitors).toBeGreaterThan(base[0].visitors);expect(changed[0].churnMrr).toBe(400);expect(changed[1].endingMrr).not.toBe(base[1].endingMrr)});it('reconciles logo and revenue churn through churned-customer ARPU',()=>{const [month]=forecast({month:'2026-07',visitors:0,customers:1000,mrr:50000},{...a,months:1,monthlyTrafficGrowth:0,signupRate:0,purchaseRate:0,voluntaryCustomerChurn:.05,delinquentCustomerChurn:0,voluntaryRevenueChurn:.03,delinquentRevenueChurn:0,expansionRate:0,retractionRate:0});expect(month.churnedCustomers).toBe(50);expect(month.churnMrr).toBe(1500);expect(month.churnedCustomerArpu).toBe(30);expect(month.churnedArpuRatio).toBe(.6)});it('keeps LTV stable when only logo churn changes and increases it when revenue churn falls',()=>{const start={month:'2026-07',visitors:1000,customers:100,mrr:4000};const base=forecast(start,{...a,months:1})[0];const lowerLogo=forecast(start,{...a,months:1,voluntaryCustomerChurn:.01})[0];const lowerRevenue=forecast(start,{...a,months:1,voluntaryRevenueChurn:.01})[0];expect(lowerLogo.ltv).toBe(base.ltv);expect(lowerRevenue.ltv).toBeGreaterThan(base.ltv!)});it('weights acquisition ARPU across baseline and active channel customers',()=>{const start={month:'2026-07',visitors:1000,customers:100,mrr:4000};const base=forecast(start,{...a,months:1})[0];const enterprise=forecast(start,{...a,months:1},[{name:'Enterprise / B2B',visitors:100,goLiveMonth:1,signupRate:.1,purchaseRate:.2,arpu:1000}])[0];expect(base.acquisitionArpu).toBe(40);expect(enterprise.acquisitionArpu).toBe(120);expect(enterprise.ltv).toBe(3000);expect(enterprise.ltv).toBeGreaterThan(base.ltv!);const inactive=forecast(start,{...a,months:1},[{name:'Enterprise / B2B',visitors:0,goLiveMonth:1,signupRate:.1,purchaseRate:.2,arpu:1000}])[0];expect(inactive.ltv).toBe(base.ltv)});it('marks churn-based unit economics unavailable when revenue churn is zero',()=>{const [month]=forecast({month:'2026-07',visitors:1000,customers:100,mrr:4000},{...a,voluntaryRevenueChurn:0,delinquentRevenueChurn:0});expect(month.ltv).toBeNull();expect(month.maxCac).toBeNull();expect(month.maxCostPerSignup).toBeNull()});it('is deterministic',()=>expect(forecast({month:'2026-07',visitors:1000,customers:100,mrr:4000},a)).toEqual(forecast({month:'2026-07',visitors:1000,customers:100,mrr:4000},a)))})
